# 🔒 SUPABASE SECURITY SETUP

## Row Level Security (RLS)

Этот проект автоматически настраивает Row Level Security для защиты данных в Supabase.

### Что настраивается:

#### 1. **Таблица User**
- ✅ RLS включен
- 🔒 Политика: Доступ только через service_role key (административные операции)
- 📝 Обычные пользователи не могут напрямую обращаться к таблице

#### 2. **Таблица News** 
- ✅ RLS включен
- 👁️ Политика: Публичное чтение только опубликованных новостей (`published = true`)
- 🔒 Административные операции через service_role key

#### 3. **Таблица Blog**
- ✅ RLS включен  
- 👁️ Политика: Публичное чтение только опубликованных постов (`published = true`)
- 🔒 Административные операции через service_role key

### Автоматическая настройка:

RLS политики применяются автоматически при запуске контейнера через:
```bash
npm run db:setup-rls
```

### Ручная настройка:

Если нужно настроить вручную в Supabase SQL Editor:

```sql
-- Включить RLS
ALTER TABLE "User" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "News" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "Blog" ENABLE ROW LEVEL SECURITY;

-- Создать политики
CREATE POLICY "Public can read published news" ON "News"
FOR SELECT USING (published = true);

CREATE POLICY "Public can read published blog posts" ON "Blog"  
FOR SELECT USING (published = true);

CREATE POLICY "System access only for users" ON "User"
FOR ALL USING (false) WITH CHECK (false);
```

### Ключи доступа:

- **anon key** - только для чтения опубликованного контента
- **service_role key** - полный доступ для административных операций (обходит RLS)

### Безопасность:

✅ **Включен RLS** для всех таблиц  
✅ **Публичный доступ** только к опубликованному контенту  
✅ **Административные операции** защищены service_role key  
✅ **Автоматическое применение** при развертывании  

### Проверка:

После развертывания проверьте в Supabase Dashboard:
1. Settings → Database → Row Level Security
2. Убедитесь что все таблицы имеют статус "RLS enabled"
3. Проверьте созданные политики в разделе Policies
