# 🔐 Полная инструкция по развертыванию на сервере

## Этап 1: Подключение к серверу

```bash
# Подключитесь к серверу по SSH
ssh root@your_server_ip
# или
ssh your_user@your_server_ip
```

---

## Этап 2: Настройка доступа к приватному GitHub репозиторию

### 2.1. Генерация SSH ключа на сервере

```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "kakb-server@raycon.kz"

# Нажмите Enter 3 раза (путь по умолчанию, без пароля)
```

### 2.2. Копирование публичного ключа

```bash
# Просмотр публичного ключа
cat ~/.ssh/id_ed25519.pub
```

Скопируйте весь вывод (начинается с `ssh-ed25519`)

### 2.3. Добавление ключа в GitHub

#### Вариант A: Deploy Key (только для этого репозитория)

1. Перейдите на https://github.com/AGGIB/kakb
2. Откройте **Settings** → **Deploy keys**
3. Нажмите **Add deploy key**
4. Вставьте скопированный ключ
5. Поставьте галочку **Allow write access** (если нужно)
6. Нажмите **Add key**

#### Вариант B: Personal Access Token (более универсально)

1. Перейдите на https://github.com/settings/tokens
2. **Generate new token** → **Generate new token (classic)**
3. Дайте название: `KAKB Server Deploy`
4. Выберите срок действия: `No expiration`
5. Права: поставьте галочку **repo** (полный доступ к репозиториям)
6. Нажмите **Generate token**
7. **СКОПИРУЙТЕ TOKEN** (он больше не отобразится!)

### 2.4. Тестирование подключения к GitHub

```bash
# Тест SSH подключения
ssh -T git@github.com

# Должно появиться:
# Hi AGGIB! You've successfully authenticated...
```

Если появляется вопрос `Are you sure you want to continue connecting?`, введите `yes`

---

## Этап 3: Установка необходимого ПО

### 3.1. Обновление системы

```bash
sudo apt update
sudo apt upgrade -y
```

### 3.2. Установка Docker

```bash
# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавление текущего пользователя в группу docker
sudo usermod -aG docker $USER

# Применение изменений (или перелогиньтесь)
newgrp docker

# Проверка установки
docker --version
```

### 3.3. Установка Docker Compose

```bash
# Установка Docker Compose plugin
sudo apt install docker-compose-plugin -y

# Проверка версии
docker compose version
```

### 3.4. Установка Git (если не установлен)

```bash
sudo apt install git -y
git --version
```

---

## Этап 4: Клонирование репозитория

### 4.1. Создание директории для проекта

```bash
# Переход в директорию для приложений
cd /opt

# Если нужны права sudo
sudo mkdir -p /opt
```

### 4.2. Клонирование через SSH

```bash
# Клонирование приватного репозитория
sudo git clone git@github.com:AGGIB/kakb.git

# Переход в директорию
cd kakb

# Дать права текущему пользователю
sudo chown -R $USER:$USER /opt/kakb
```

### 4.3. Альтернатива: клонирование через HTTPS с токеном

Если используете Personal Access Token:

```bash
# Замените YOUR_TOKEN на ваш токен
sudo git clone https://YOUR_TOKEN@github.com/AGGIB/kakb.git

cd kakb
sudo chown -R $USER:$USER /opt/kakb
```

### 4.4. Проверка клонирования

```bash
# Проверка содержимого
ls -la

# Должны увидеть:
# Dockerfile
# docker-compose.yml
# package.json
# и другие файлы
```

---

## Этап 5: Настройка окружения (опционально)

### 5.1. Создание файла .env (если нужно)

```bash
# Создание файла с переменными окружения
cat > .env << 'EOF'
NODE_ENV=production
NEXT_PUBLIC_SITE_URL=http://kakb.raycon.kz
EOF
```

---

## Этап 6: Сборка и запуск приложения

### 6.1. Сборка Docker образов

```bash
# Сборка образов (займет 3-5 минут)
docker compose build

# Следите за процессом, убедитесь, что нет ошибок
```

### 6.2. Запуск контейнеров

```bash
# Запуск в фоновом режиме
docker compose up -d
```

### 6.3. Проверка статуса контейнеров

```bash
# Проверка запущенных контейнеров
docker compose ps

# Должны увидеть 2 контейнера:
# kakb-website   (running)
# kakb-nginx     (running)
```

### 6.4. Просмотр логов

```bash
# Просмотр логов в реальном времени
docker compose logs -f

# Для выхода нажмите Ctrl+C
```

---

## Этап 7: Проверка работы сайта

### 7.1. Проверка локально на сервере

```bash
# Проверка через curl
curl http://localhost

# Должен вернуться HTML код страницы
```

### 7.2. Проверка портов

```bash
# Проверка, что порт 80 слушается
sudo netstat -tulpn | grep :80

# Или
sudo ss -tulpn | grep :80
```

### 7.3. Открытие в браузере

Откройте в браузере: **http://kakb.raycon.kz**

Если DNS настроен правильно, сайт должен открыться! 🎉

---

## Этап 8: Настройка автозапуска (опционально)

### 8.1. Создание systemd сервиса

```bash
sudo tee /etc/systemd/system/kakb.service > /dev/null << 'EOF'
[Unit]
Description=KAKB Website Docker Compose
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/opt/kakb
ExecStart=/usr/bin/docker compose up -d
ExecStop=/usr/bin/docker compose down
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
EOF
```

### 8.2. Активация автозапуска

```bash
# Перезагрузка systemd
sudo systemctl daemon-reload

# Включение автозапуска
sudo systemctl enable kakb

# Запуск сервиса
sudo systemctl start kakb

# Проверка статуса
sudo systemctl status kakb
```

---

## Этап 9: Настройка SSL (HTTPS) - опционально

### 9.1. Установка Certbot

```bash
sudo apt install certbot python3-certbot-nginx -y
```

### 9.2. Получение SSL сертификата

```bash
# Остановка Nginx на время получения сертификата
docker compose stop nginx

# Получение сертификата
sudo certbot certonly --standalone -d kakb.raycon.kz

# Запуск Nginx обратно
docker compose start nginx
```

### 9.3. Обновление nginx.conf для SSL

Нужно будет обновить `nginx.conf` и добавить SSL конфигурацию.

---

## 🔄 Обновление приложения после изменений в коде

### Быстрое обновление:

```bash
cd /opt/kakb

# Получение последних изменений
git pull origin main

# Пересборка и перезапуск
docker compose down
docker compose build --no-cache
docker compose up -d

# Проверка логов
docker compose logs -f
```

---

## 🛠 Полезные команды для управления

### Просмотр логов

```bash
# Все логи
docker compose logs -f

# Только Next.js приложение
docker compose logs -f kakb-website

# Только Nginx
docker compose logs -f nginx

# Последние 100 строк
docker compose logs --tail=100
```

### Перезапуск контейнеров

```bash
# Перезапуск всех контейнеров
docker compose restart

# Перезапуск конкретного контейнера
docker compose restart kakb-website
```

### Остановка и удаление

```bash
# Остановка контейнеров
docker compose stop

# Остановка и удаление контейнеров
docker compose down

# Удаление с volume (ОСТОРОЖНО!)
docker compose down -v
```

### Мониторинг ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Дисковое пространство Docker
docker system df

# Общее дисковое пространство
df -h
```

---

## ⚠️ Решение проблем

### Проблема: "Permission denied" при git clone

```bash
# Проверьте SSH ключ
ssh -T git@github.com

# Если не работает, используйте HTTPS с токеном
git clone https://YOUR_TOKEN@github.com/AGGIB/kakb.git
```

### Проблема: Порт 80 занят

```bash
# Проверка, что занимает порт 80
sudo netstat -tulpn | grep :80

# Остановка процесса (например, Apache)
sudo systemctl stop apache2
# или
sudo systemctl stop nginx
```

### Проблема: Контейнер не запускается

```bash
# Просмотр детальных логов
docker compose logs -f kakb-website

# Проверка статуса
docker compose ps

# Пересборка с нуля
docker compose down
docker system prune -a -f
docker compose build --no-cache
docker compose up -d
```

### Проблема: Сайт не открывается

```bash
# 1. Проверка контейнеров
docker compose ps

# 2. Проверка логов
docker compose logs -f

# 3. Проверка портов
sudo netstat -tulpn | grep :80

# 4. Проверка локально
curl http://localhost

# 5. Проверка DNS
ping kakb.raycon.kz
```

---

## 🔒 Безопасность

### Базовые рекомендации:

1. **Firewall**
```bash
# Установка ufw
sudo apt install ufw -y

# Разрешение SSH
sudo ufw allow ssh

# Разрешение HTTP
sudo ufw allow 80/tcp

# Разрешение HTTPS (если используете)
sudo ufw allow 443/tcp

# Включение firewall
sudo ufw enable
```

2. **Обновления**
```bash
# Автоматические обновления безопасности
sudo apt install unattended-upgrades -y
sudo dpkg-reconfigure -plow unattended-upgrades
```

3. **Регулярный мониторинг**
```bash
# Просмотр логов Docker
docker compose logs --tail=100

# Системные логи
sudo journalctl -xe
```

---

## 📞 Контакты и поддержка

- **Репозиторий**: https://github.com/AGGIB/kakb
- **Email**: info@kakb.kz
- **Домен**: http://kakb.raycon.kz

---

## ✅ Чеклист развертывания

- [ ] Подключились к серверу
- [ ] Сгенерировали SSH ключ
- [ ] Добавили ключ в GitHub (Deploy Key или Token)
- [ ] Установили Docker
- [ ] Установили Docker Compose
- [ ] Клонировали репозиторий
- [ ] Собрали Docker образы
- [ ] Запустили контейнеры
- [ ] Проверили статус контейнеров
- [ ] Открыли сайт в браузере
- [ ] (Опционально) Настроили автозапуск
- [ ] (Опционально) Настроили SSL

**Готово! Сайт работает! 🎉**
