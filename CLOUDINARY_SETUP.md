# 🖼️ Настройка загрузки изображений (Cloudinary)

## Зачем нужно?

Теперь в админке можно загружать изображения напрямую, без вставки URL!
- ✅ Загрузка в высоком качестве (без сжатия)
- ✅ Простой drag & drop
- ✅ Бесплатно до 25GB хранилища

---

## Шаг 1: Регистрация на Cloudinary

### 1.1 Создайте аккаунт
1. Перейдите на https://cloudinary.com/
2. Нажмите **"Sign Up For Free"**
3. Зарегистрируйтесь (можно через Google/GitHub)

### 1.2 Получите учетные данные
После входа в аккаунт вы увидите Dashboard:

1. Найдите секцию **"Account Details"** или **"API Keys"**
2. Скопируйте три значения:
   - **Cloud Name** (например: `dxxx12345`)
   - **API Key** (например: `123456789012345`)
   - **API Secret** (например: `abcdefghijklmnopqrstuvwxyz`)

---

## Шаг 2: Настройка локально

### 2.1 Обновите файл `.env`

Добавьте в конец файла `.env`:

```env
# Cloudinary (для загрузки изображений)
CLOUDINARY_CLOUD_NAME="ваш-cloud-name"
CLOUDINARY_API_KEY="ваш-api-key"
CLOUDINARY_API_SECRET="ваш-api-secret"
```

**Пример:**
```env
CLOUDINARY_CLOUD_NAME="dxxx12345"
CLOUDINARY_API_KEY="123456789012345"
CLOUDINARY_API_SECRET="abcdefghijklmnopqrstuvwxyz"
```

### 2.2 Перезапустите сервер

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем снова запустите:
npm run dev
```

---

## Шаг 3: Тестируем загрузку

1. Откройте админку: `http://localhost:3000/admin/login`
2. Войдите в систему
3. Перейдите в **"Новости"** → **"Добавить новость"**
4. В секции **"Изображение"**:
   - Нажмите **"Загрузить изображение"**
   - Выберите файл (PNG, JPG, GIF до 10MB)
   - Дождитесь загрузки
   - ✅ Изображение загружено!

5. Заполните остальные поля и опубликуйте новость
6. Проверьте что изображение отображается на странице `/news`

---

## Шаг 4: Настройка для продакшена (Vercel)

### 4.1 Добавьте переменные окружения в Vercel

1. Зайдите в **Vercel Dashboard**
2. Выберите ваш проект
3. Перейдите в **Settings → Environment Variables**
4. Добавьте три переменные:

| Name | Value |
|------|-------|
| `CLOUDINARY_CLOUD_NAME` | Ваш Cloud Name |
| `CLOUDINARY_API_KEY` | Ваш API Key |
| `CLOUDINARY_API_SECRET` | Ваш API Secret |

5. Нажмите **"Save"** для каждой переменной

### 4.2 Передеплойте проект

```bash
git add .
git commit -m "Add image upload with Cloudinary"
git push
```

Vercel автоматически пересоберет проект с новыми переменными.

---

## 📋 Как это работает?

### Загрузка изображения:
1. Пользователь выбирает файл в админке
2. Файл отправляется на `/api/upload`
3. API конвертирует файл в base64
4. Загружает в Cloudinary **БЕЗ СЖАТИЯ** (quality: 'auto:best')
5. Cloudinary возвращает постоянный URL
6. URL сохраняется в базе данных

### Отображение изображения:
- URL хранится в базе данных
- На публичных страницах используется компонент `Image` от Next.js
- Добавлен флаг `unoptimized` чтобы не сжимать повторно

---

## 🎨 Настройки качества

В файле `app/api/upload/route.ts` настроены:

```typescript
quality: 'auto:best',  // Максимальное качество
format: 'auto',        // Оригинальный формат
transformation: [],    // Без трансформаций
```

**Это гарантирует:**
- ✅ Оригинальное качество изображения
- ✅ Без сжатия
- ✅ Без изменения размеров

---

## 💰 Бесплатный лимит Cloudinary

**План Free:**
- 25 GB хранилища
- 25 GB bandwidth (трафик) в месяц
- 25,000 трансформаций в месяц

**Для твоего сайта этого более чем достаточно!**

---

## ⚠️ Ограничения

Текущие настройки:
- Максимальный размер файла: **10 MB**
- Поддерживаемые форматы: **PNG, JPG, GIF, WEBP**
- Папка на Cloudinary: **kakb/**

Если нужно изменить - редактируй `app/api/upload/route.ts`

---

## 🛠️ Troubleshooting

### Ошибка: "Файл не найден"
- Убедитесь что выбрали изображение (не другой тип файла)

### Ошибка: "Размер файла превышает 10MB"
- Сожмите изображение перед загрузкой
- Или увеличьте лимит в `image-upload.tsx`

### Ошибка: "Ошибка загрузки изображения"
- Проверьте переменные окружения в `.env`
- Убедитесь что CLOUDINARY_* правильно заполнены
- Проверьте консоль браузера на ошибки

### Изображение не отображается
- Откройте ссылку на изображение в новой вкладке
- Если открывается - проблема в коде отображения
- Если не открывается - проблема с загрузкой на Cloudinary

---

## ✨ Готово!

Теперь администраторы могут:
- 🖼️ Загружать изображения напрямую
- 📸 В высоком качестве (без сжатия)
- 💨 Drag & drop или выбор файла
- 🗑️ Удалять и заменять изображения

**Никаких URL - просто загружай и публикуй!** 🎉
