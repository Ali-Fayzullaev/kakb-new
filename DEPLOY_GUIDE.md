# 🚀 Гайд по деплою на Vercel с PostgreSQL

## Шаг 1: Создание PostgreSQL базы данных (Neon)

### 1.1 Регистрация на Neon
1. Перейди на https://neon.tech
2. Нажми "Sign Up" (можно через GitHub)
3. Войди в аккаунт

### 1.2 Создание проекта
1. Нажми **"Create a project"**
2. Выбери регион (ближайший к тебе, например: `AWS / EU (Frankfurt)`)
3. Дай название проекту: `kakb-production`
4. Нажми **"Create project"**

### 1.3 Получение DATABASE_URL
После создания проекта:
1. Увидишь **Connection String**
2. Скопируй строку, которая начинается с `postgresql://`
3. Выглядит примерно так:
   ```
   postgresql://username:password@ep-xxx.eu-central-1.aws.neon.tech/neondb?sslmode=require
   ```

---

## Шаг 2: Обновление локального проекта

### 2.1 Обновляем схему Prisma

Файл `prisma/schema.prisma` уже настроен на PostgreSQL! ✅

### 2.2 Создаем новый .env файл

Создай файл `.env.production` (для проверки):
```env
DATABASE_URL="postgresql://твой-url-из-neon"
NEXTAUTH_SECRET="сгенерируй-длинный-случайный-ключ-минимум-32-символа"
NEXTAUTH_URL="http://localhost:3000"
```

**Генерация NEXTAUTH_SECRET:**
В PowerShell:
```powershell
[Convert]::ToBase64String([System.Text.Encoding]::UTF8.GetBytes((New-Guid).ToString() + (New-Guid).ToString()))
```

### 2.3 Обновляем .env для локальной разработки

Твой `.env` файл:
```env
# Для локальной разработки
DATABASE_URL="file:./dev.db"

# Для продакшена закомментировано
# DATABASE_URL="postgresql://..."
```

---

## Шаг 3: Тестируем миграцию локально с PostgreSQL

### 3.1 Временно переключаемся на PostgreSQL

В `.env` замени на свой Neon URL:
```env
DATABASE_URL="postgresql://твой-url-из-neon"
```

### 3.2 Запускаем миграцию

```bash
npx prisma migrate deploy
```

Или создаем новую миграцию:
```bash
npx prisma migrate dev --name init_postgres
```

### 3.3 Создаем администратора на продакшен БД

```bash
npm run create-admin
```

### 3.4 Проверяем что всё работает

```bash
npm run dev
```

Зайди в админку, создай тестовую новость, проверь что она сохраняется.

---

## Шаг 4: Деплой на Vercel

### 4.1 Устанавливаем Vercel CLI (опционально)

```bash
npm i -g vercel
```

### 4.2 Деплой через GitHub (рекомендую)

1. **Залей код на GitHub:**
   ```bash
   git add .
   git commit -m "Add admin panel with PostgreSQL"
   git push
   ```

2. **Подключи к Vercel:**
   - Зайди на https://vercel.com
   - Нажми "Add New Project"
   - Выбери свой GitHub репозиторий
   - Нажми "Import"

### 4.3 Настройка переменных окружения в Vercel

В настройках проекта Vercel:

1. Перейди в **Settings → Environment Variables**
2. Добавь переменные:

| Name | Value |
|------|-------|
| `DATABASE_URL` | `postgresql://твой-url-из-neon` |
| `NEXTAUTH_SECRET` | `твой-сгенерированный-секрет` |
| `NEXTAUTH_URL` | `https://твой-домен.vercel.app` |

3. Нажми **"Save"** для каждой переменной

### 4.4 Деплой

Нажми **"Deploy"** - Vercel автоматически:
- Установит зависимости
- Соберет проект
- Запустит Prisma генерацию
- Задеплоит сайт

---

## Шаг 5: После деплоя

### 5.1 Проверка

1. Открой `https://твой-сайт.vercel.app/admin/login`
2. Войди с данными:
   - Email: `admin@kakb.kz`
   - Пароль: `Admin123!`
3. Создай тестовую новость
4. Проверь что она отображается на `/news`

### 5.2 Настройка своего домена (опционально)

В Vercel → Settings → Domains:
1. Добавь свой домен (например: `kakb.kz`)
2. Следуй инструкциям для настройки DNS
3. Обнови `NEXTAUTH_URL` на свой домен

---

## 🔧 Важные команды

### Локальная разработка (SQLite)
```bash
# .env должен содержать:
# DATABASE_URL="file:./dev.db"

npm run dev
npm run db:migrate
npm run db:studio
```

### Продакшен (PostgreSQL)
```bash
# .env должен содержать PostgreSQL URL

npx prisma migrate deploy  # Применить миграции
npm run create-admin        # Создать админа
```

---

## ⚠️ Troubleshooting

### Ошибка: "Can't reach database server"
- Проверь правильность `DATABASE_URL`
- Убедись что в конце есть `?sslmode=require`

### Ошибка при миграции
```bash
npx prisma migrate reset  # Сбросить БД (ОСТОРОЖНО!)
npx prisma migrate deploy # Применить миграции
```

### Админ не может войти
```bash
npm run create-admin  # Пересоздать админа
```

### Данные не сохраняются после деплоя
- Убедись что `DATABASE_URL` в Vercel указывает на PostgreSQL
- Проверь что миграции применены: `npx prisma migrate deploy`

---

## 📋 Чеклист перед деплоем

- [ ] PostgreSQL база создана на Neon
- [ ] `DATABASE_URL` скопирован из Neon
- [ ] Миграции применены: `npx prisma migrate deploy`
- [ ] Администратор создан: `npm run create-admin`
- [ ] Код залит на GitHub
- [ ] Проект импортирован в Vercel
- [ ] Переменные окружения добавлены в Vercel:
  - [ ] `DATABASE_URL`
  - [ ] `NEXTAUTH_SECRET`
  - [ ] `NEXTAUTH_URL`
- [ ] Деплой завершен успешно
- [ ] Вход в админку работает
- [ ] Создание/удаление новостей работает

---

## 🎉 Готово!

Теперь твой сайт:
- ✅ Работает на продакшене
- ✅ Использует PostgreSQL
- ✅ Данные не теряются при деплое
- ✅ Админка доступна 24/7
- ✅ Можно управлять контентом из любого места

---

## 🔄 Workflow после деплоя

### Локальная разработка
```bash
# Работаешь с SQLite локально
npm run dev
```

### Деплой изменений
```bash
git add .
git commit -m "Update feature"
git push
# Vercel автоматически задеплоит
```

### Работа с админкой
Просто открываешь:
```
https://твой-сайт.vercel.app/admin/login
```

**Удачи с деплоем! 🚀**
